export type State = {
  homeContent: any;
  token: string;
};

export type Link = {
  href: string;
  name: string;
};

export type HomeContent = {
  summary: string;
  contact: string;
  linkedInURL: string;
};
